config center root:

唯一的中心节点服务. 
用于获取配置中心的地址列表.

它的数据只通过配置文件或间接的使用数据库进行加载, 没有注册或动态变更的情况.
只使用UDP, 不使用TCP.

对于其它服务而言, 它所返回的配置中心, 不一定是真实的配置中心地址. 而是做任意"相当于配置中心"的地址.

配置中心的服务器数量必然不多, (参见代码限制).
因此单个包必然能返回完整的配置中心列表.
